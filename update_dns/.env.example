# .env.example
#
# This file serves as a template for the environment configuration for the
# network_autopilot.py script and related services. Copy this template file to ".env",
# then update the values as necessary.
#
############
# IMPORTANT:
############
# - Do not git commit your actual .env file with sensitive data.
#
##########################
# Configuration Variables:
##########################
# 
# CLOUDFLARE_API_BASE_URL: The base API endpoint for Cloudflare requests
#    - Default: https://api.cloudflare.com/client/v4
#    - Only change this if you are using a different API gateway
#
# CLOUDFLARE_API_TOKEN: Your Cloudflare user API token for DNS management
#    - Generate in Cloudflare Dashboard -> My Profile -> API Tokens -> Create Token
#    - Keep this value private
#
# CLOUDFLARE_ZONE_ID: The unique zone ID of your Cloudflare domain
#    - Found in Cloudflare Dashboard -> Your Domain -> Overview -> API section
#
# CLOUDFLARE_DNS_NAME: The unique DNS record (sub-domain) to update
#    - Retrieve via Cloudflare API or dashboard when editing that DNS record 
#
# GOOGLE_SHEET_NAME: The name of your Google Sheet used for logging IP addresses of current device
#    - Used in: src/network_autopilot/network_autopilot.py
# 
# GOOGLE_WORKSHEET: The specific worksheet (tab) inside the Google Sheet
#    - Used in: src/network_autopilot/network_autopilot.py
# 
# GOOGLE_API_KEY_LOCAL: Define absolute API key path for local testing
#    - Used in: src/network_autopilot/network_autopilot.py (for authenticating Google APIs)
#    - Used in: docker-compose.yaml (mounted as a volume)
#
# GOOGLE_API_KEY_DOCKER: Define API key mount location inside Docker
#    - Used in: src/network_autopilot/network_autopilot.py (for authenticating Google APIs) ?????????????????
#    - Used in: docker-compose.yaml (mounted as a volume) ?????????????????
#
# TZ: Controls the local timezone used for formatting Cloudflare "last modified" DNS record timestamps
#    - Used in src/network_autopilot/network_autopilot.py
#    - Used in Docker container env (Dockerfile + docker-compose.yaml)
#
# LOG_PATH: The path to the logs
#    - Used in: src/network_autopilot/network_autopilot.py (not currently supported, placeholder for future development)
# 
##########
# Example:
##########
# --- Cloudflare ---
# CLOUDFLARE_API_BASE_URL="https://api.cloudflare.com/client/v4"
# CLOUDFLARE_API_TOKEN="1234567890abcdef"
# CLOUDFLARE_ZONE_ID="abcdef1234567890"
# CLOUDFLARE_DNS_NAME="vpn.example.com"

# --- Google ---
# GOOGLE_SHEET_NAME="My Google Sheet"
# GOOGLE_WORKSHEET="My Google Worksheet Tab"
# GOOGLE_API_KEY_LOCAL=~/.config/api_keys/google_services_api_key.json
# GOOGLE_API_KEY_DOCKER=/creds/google_services_api_key.json

# --- Hardware ---
# ROUTER_IP="192.168.0.1"
# PLUG_IP="192.168.0.150"
# REBOOT_DELAY=30
# INIT_DELAY=30

# TZ=America/New_York   #used in the docker container then main script derives from it
# LOG_PATH=""
